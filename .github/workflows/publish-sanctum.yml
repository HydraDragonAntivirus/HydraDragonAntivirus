name: Publish Sanctum

on:
  push:
    branches: ["**"]
    paths:
      - 'Sanctum/**'
permissions:
  contents: write
  pull-requests: write

jobs:
  build-and-stage:
    runs-on: windows-2022
    steps:
      - name: Enable Git long paths
        run: git config --global core.longpaths true
      - uses: actions/checkout@v6
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@nightly
      - name: Install Cargo Make
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-make
      - name: Install rust-script
        run: cargo install rust-script
      - name: Setup WDK
        shell: pwsh
        run: |
          $wdkSetupPath = "$Env:TEMP\wdksetup.exe"
          (New-Object Net.WebClient).DownloadFile('https://go.microsoft.com/fwlink/?linkid=2335869', $wdkSetupPath)
          Start-Process -FilePath $wdkSetupPath -ArgumentList "/quiet" -NoNewWindow -Wait
      
      - name: Build and Sign
        working-directory: Sanctum
        run: |
          cd driver
          .\cert.ps1
          cargo make release
          .\sign.bat release
          cd ..
          cargo build --release -p sanctum_ppl_runner
          .\sign_ppl_runner.bat
          cargo make release
          
      - name: Stage
        shell: pwsh
        run: |
          $dist = "hydradragon/Sanctum"
          New-Item -ItemType Directory -Force -Path $dist
          
          # Use recursive lookup for driver and usermode binaries to avoid folder name mismatches
          Get-ChildItem "Sanctum/driver/target/" -Filter "sanctum.sys" -Recurse | Select-Object -First 1 | Copy-Item -Destination "$dist/" -Force
          Get-ChildItem "Sanctum/target/release/" -Filter "sanctum_ppl_runner.exe" -Recurse | Select-Object -First 1 | Copy-Item -Destination "$dist/" -Force
          Get-ChildItem "Sanctum/target/release/" -Filter "elam_installer.exe" -Recurse | Select-Object -First 1 | Copy-Item -Destination "$dist/" -Force
          Get-ChildItem "Sanctum/target/release/" -Filter "um_engine.exe" -Recurse | Select-Object -First 1 | Copy-Item -Destination "$dist/" -Force
          Get-ChildItem "Sanctum/target/release/" -Filter "sanctum.dll" -Recurse | Select-Object -First 1 | Copy-Item -Destination "$dist/" -Force
          
      - name: Create PR
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(sanctum): update binaries"
          title: "🚀 Update Sanctum Binaries"
          branch: "auto-update-sanctum"
          base: ${{ github.ref_name }}
          delete-branch: true
