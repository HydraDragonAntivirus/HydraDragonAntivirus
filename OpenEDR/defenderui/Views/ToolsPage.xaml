<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="DefenderUI.Views.ToolsPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:DefenderUI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:models="using:DefenderUI.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Padding="{StaticResource PaddingPage}" Spacing="{StaticResource Space2xl}">

            <!-- Header -->
            <StackPanel Spacing="4">
                <TextBlock Text="Araçlar"
                           Style="{StaticResource DisplayMediumTextStyle}"
                           AutomationProperties.HeadingLevel="Level1" />
                <TextBlock Text="Sisteminizi optimize edin ve bakımını yapın"
                           Style="{StaticResource BodyLargeTextStyle}"
                           Foreground="{ThemeResource TextSecondaryBrush}" />
            </StackPanel>

            <!-- Tools Grid -->
            <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Tools, Mode=OneWay}">
                <muxc:ItemsRepeater.Layout>
                    <muxc:UniformGridLayout
                        MinItemWidth="280"
                        MinItemHeight="160"
                        MinColumnSpacing="16"
                        MinRowSpacing="16"
                        ItemsStretch="Fill" />
                </muxc:ItemsRepeater.Layout>
                <muxc:ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:FeatureTileData">
                        <controls:FeatureTileCard
                            Glyph="{x:Bind Glyph}"
                            Title="{x:Bind Title}"
                            Description="{x:Bind Description}"
                            BadgeText="{x:Bind BadgeText}"
                            ActionText="Aç"
                            Command="{Binding ElementName=RootPage, Path=ViewModel.LaunchToolCommand}"
                            CommandParameter="{x:Bind NavigateKey}" />
                    </DataTemplate>
                </muxc:ItemsRepeater.ItemTemplate>
            </muxc:ItemsRepeater>

        </StackPanel>
    </ScrollViewer>
</Page>