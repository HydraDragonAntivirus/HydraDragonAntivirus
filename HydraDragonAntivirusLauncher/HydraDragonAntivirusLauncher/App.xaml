<Application x:Class="HydraDragonAntivirusLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ==== Color primitives (Colors used in GradientStops) ==== -->
        <Color x:Key="AccentLightColor">#FF81A1C1</Color>
        <Color x:Key="AccentDarkColor">#FF6B8DB8</Color>
        <Color x:Key="ActionTopColor">#FF5E81AC</Color>
        <Color x:Key="ActionBottomColor">#FF5273A0</Color>
        <Color x:Key="WindowGradTopColor">#FF3B4252</Color>
        <Color x:Key="WindowGradBottomColor">#FF2E3440</Color>
        <Color x:Key="TextColorColor">#FFECEFF4</Color>

        <!-- ==== Brushes (for Background, Foreground, etc.) ==== -->
        <SolidColorBrush x:Key="TextColor" Color="{StaticResource TextColorColor}"/>
        <SolidColorBrush x:Key="AccentLight" Color="{StaticResource AccentLightColor}"/>
        <SolidColorBrush x:Key="AccentDark" Color="{StaticResource AccentDarkColor}"/>
        <SolidColorBrush x:Key="ActionTopBrush" Color="{StaticResource ActionTopColor}"/>
        <SolidColorBrush x:Key="ActionBottomBrush" Color="{StaticResource ActionBottomColor}"/>

        <!-- ==== Window background gradient (used in MainWindow Background) ==== -->
        <LinearGradientBrush x:Key="WindowGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource WindowGradTopColor}" Offset="0" />
            <GradientStop Color="{StaticResource WindowGradBottomColor}" Offset="1" />
        </LinearGradientBrush>

        <!-- ==== Effects ==== -->
        <DropShadowEffect x:Key="WindowShadow" Color="#FF000000" Direction="320" ShadowDepth="5" Opacity="0.50" BlurRadius="10"/>
        <DropShadowEffect x:Key="SubtleShadow" Color="#FF000000" Direction="320" ShadowDepth="3" Opacity="0.35" BlurRadius="8"/>
        <DropShadowEffect x:Key="HoverGlow" Color="#FF88C0D0" Direction="0" ShadowDepth="0" Opacity="0.8" BlurRadius="18"/>

        <!-- ==== Action gradient built from Color primitives - used as default Background for action buttons ==== -->
        <LinearGradientBrush x:Key="ActionGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="{StaticResource ActionTopColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource ActionBottomColor}" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- ==== Base Button Template (for consistent behavior) ==== -->
        <Style x:Key="ActionButtonStyleBase" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd"
                                    CornerRadius="10"
                                    Background="{TemplateBinding Background}"
                                    Effect="{TemplateBinding Effect}"
                                    SnapsToDevicePixels="True"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"/>
                            </Border>

                            <Border x:Name="FocusVisual" CornerRadius="10" BorderThickness="2" Visibility="Collapsed" IsHitTestVisible="False">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="#FF88C0D0" Opacity="0.18"/>
                                </Border.BorderBrush>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Effect" Value="{StaticResource HoverGlow}"/>
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="{StaticResource AccentLightColor}" Offset="0.0"/>
                                            <GradientStop Color="{StaticResource AccentDarkColor}" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.02" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.02" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.0" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.0" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Effect" Value="{x:Null}"/>
                                <Setter TargetName="Bd" Property="Background" Value="#FF4C6A94"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="0.97" Duration="0:0:0.06"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="0.97" Duration="0:0:0.06"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.0" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.0" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="FocusVisual" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==== Epic style (looks like your python #action_button by default) ==== -->
        <Style x:Key="EpicButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyleBase}">
            <Setter Property="Background" Value="{StaticResource ActionGradient}"/>
            <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
            <Setter Property="Padding" Value="16,10"/>
        </Style>

        <!-- ==== Optional: Danger style ==== -->
        <LinearGradientBrush x:Key="DangerGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFBF616A" Offset="0.0"/>
            <GradientStop Color="#FFB34C56" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyleBase}">
            <Setter Property="Background" Value="{StaticResource DangerGradient}"/>
            <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
        </Style>

        <!-- ==== Implicit global Button style (so plain <Button/> uses Epic look) ==== -->
        <Style TargetType="Button" BasedOn="{StaticResource EpicButtonStyle}" />

    </Application.Resources>
</Application>
